export const CELL_SIZE = 20;
export const CELL_PADDING = 2;

export const MEM_WIDTH = 96;
export const MEM_HEIGHT = 64;
export const MEM_SIZE = MEM_WIDTH * MEM_HEIGHT;

export const CANVAS_WIDTH = MEM_WIDTH * (CELL_SIZE + CELL_PADDING);
export const CANVAS_HEIGHT = MEM_HEIGHT * (CELL_SIZE + CELL_PADDING);

export const COLORS = ["#DF2935", "#3772FF", "#FDCA40", "#7EA172"];
